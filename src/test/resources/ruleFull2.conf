rules =
  """
  name:rule1
  given:col1, col2, col3
  case:
    col1 && col2 ->
      col3
  otherwise ->
    col1 * col2
  rules:
    name: rule2
    case:
      col3 && col2 || col1 in (10, 20, 30) ->
        col3 * col2
      col1 && col3 between (100, 200) ->
        let(100)
      otherwise ->
        col4 - 1000
  get: col1, col2, col3 as boolean, rule2, rule3
  """